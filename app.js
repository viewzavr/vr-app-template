/* Hello!

This file is loaded by index.html, you may enter your app code here.
This method is just for example and all is up to you.

Example codes:

var pts = vz.vis.addPoints();
pts.positions = [1,2,3, 1,2,5, 1,3,12];

var lins = vz.vis.addLines();
lins.positions = [1,2,3, 1,2,5, 1,2,5, 1,3,12];

vz.restoreFromHash(); // this restores app state from url (e.g. all parameters and manually added items)

*/

console.log("hello Viewzavr!");
